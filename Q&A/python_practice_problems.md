# Python 실무 연습문제 모음 📚

## 시작하기 전에

### 필요한 라이브러리 설치
```bash
pip install requests pandas openpyxl
```

### 연습 파일 준비
이 문서와 함께 제공되는 예제 데이터 파일들을 같은 폴더에 두고 시작하세요.

---

## 📖 Section 1: 빌트인 모듈 연습

### 1-1. JSON 데이터 처리 (`json` 모듈)

**배경 지식:**
JSON(JavaScript Object Notation)은 데이터를 저장하고 전송하는 가장 흔한 포맷입니다.
웹 API, 설정 파일, 데이터 교환 등에서 널리 사용됩니다.

**기본 사용법:**
```python
import json

# 파일에서 JSON 읽기
with open('data.json', 'r', encoding='utf-8') as f:
    data = json.load(f)  # 딕셔너리나 리스트로 변환됨

# 파일에 JSON 쓰기
with open('output.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
    # ensure_ascii=False: 한글 깨짐 방지
    # indent=2: 보기 좋게 들여쓰기

# 문자열 <-> 딕셔너리 변환
json_str = json.dumps({'name': '홍길동', 'age': 25})
data = json.loads(json_str)
```

**문제 1-1: 학생 성적 필터링**

`students.json` 파일을 읽어서 다음을 수행하세요:
1. 평균 점수(math, english, science의 평균)가 80점 이상인 학생들만 필터링
2. 각 학생의 평균 점수를 새로운 필드로 추가
3. 결과를 `high_scores.json`에 저장
4. 조건을 만족하는 학생 수와 전체 평균 점수를 출력

**출력 예시:**
```
고득점 학생 수: 3명
전체 평균: 85.67점
```

---

### 1-2. CSV 데이터 처리 (`csv` 모듈)

**배경 지식:**
CSV(Comma-Separated Values)는 엑셀, 구글 시트 등에서 데이터를 저장할 때 사용하는 포맷입니다.
간단하지만 실무에서 가장 자주 다루는 파일 형식입니다.

**기본 사용법:**
```python
import csv

# CSV 읽기 - 딕셔너리 형태로
with open('data.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row['name'], row['age'])  # 컬럼명으로 접근

# CSV 쓰기
data = [
    {'name': '홍길동', 'score': 95},
    {'name': '김철수', 'score': 88}
]

with open('output.csv', 'w', encoding='utf-8', newline='') as f:
    fieldnames = ['name', 'score']
    writer = csv.DictWriter(f, fieldnames=fieldnames)
    writer.writeheader()  # 헤더 쓰기
    writer.writerows(data)  # 데이터 쓰기
```

**문제 1-2: 매출 데이터 분석**

`sales.csv` 파일을 읽어서:
1. 각 제품(product)별 총 매출액(price × quantity) 계산
2. 매출액이 높은 순서로 정렬
3. 상위 3개 제품을 `top_products.csv`에 저장 (컬럼: product, total_sales)
4. 전체 매출액 합계 출력

**출력 예시:**
```
전체 매출액: 1,250,000원
상위 제품이 top_products.csv에 저장되었습니다.
```

---

### 1-3. 날짜/시간 처리 (`datetime` 모듈)

**배경 지식:**
날짜와 시간 계산은 로그 분석, 일정 관리, 데이터 필터링 등에서 필수적입니다.

**기본 사용법:**
```python
from datetime import datetime, timedelta

# 현재 날짜/시간
now = datetime.now()
print(now)  # 2024-01-15 14:30:25.123456

# 문자열 -> 날짜 객체
date_str = '2024-01-15'
date = datetime.strptime(date_str, '%Y-%m-%d')

# 날짜 객체 -> 문자열
formatted = date.strftime('%Y년 %m월 %d일')  # '2024년 01월 15일'

# 날짜 계산
tomorrow = now + timedelta(days=1)
week_ago = now - timedelta(weeks=1)

# 날짜 비교
if date1 < date2:
    print("date1이 더 이전입니다")

# 날짜 차이 계산
diff = date2 - date1
print(diff.days)  # 일수 차이
```

**날짜 포맷 문자열:**
- `%Y`: 4자리 연도 (2024)
- `%m`: 2자리 월 (01-12)
- `%d`: 2자리 일 (01-31)
- `%H`: 24시간 형식 시간 (00-23)
- `%M`: 분 (00-59)
- `%S`: 초 (00-59)

**문제 1-3: 다가오는 이벤트 찾기**

`events.json` 파일을 읽어서:
1. 오늘 날짜 기준으로 7일 이내에 있는 이벤트만 필터링
2. 각 이벤트에 D-Day 정보를 추가 (예: "D-3", "D-Day", "D+2")
3. D-Day가 가까운 순서로 정렬
4. 결과를 `upcoming_events.json`에 저장

**힌트:**
- 이미 지난 이벤트는 제외
- "D-Day"는 오늘, "D-1"은 내일, "D+1"은 어제

---

### 1-4. 정규표현식 (`re` 모듈)

**배경 지식:**
정규표현식(Regular Expression)은 텍스트에서 패턴을 찾고 처리하는 강력한 도구입니다.
이메일 추출, 전화번호 포맷 검증, 로그 파싱 등에 사용됩니다.

**기본 사용법:**
```python
import re

# 패턴 찾기
text = "연락처: 010-1234-5678, 이메일: test@example.com"

# 찾기
phone = re.search(r'\d{3}-\d{4}-\d{4}', text)
if phone:
    print(phone.group())  # '010-1234-5678'

# 모두 찾기
emails = re.findall(r'[\w\.-]+@[\w\.-]+\.\w+', text)
print(emails)  # ['test@example.com']

# 치환
cleaned = re.sub(r'\d{3}-\d{4}-\d{4}', '[전화번호]', text)

# 분리
parts = re.split(r'[,\s]+', "사과, 바나나   포도")
```

**자주 사용하는 패턴:**
- `\d`: 숫자 한 자리
- `\w`: 문자 (알파벳, 숫자, _)
- `\s`: 공백
- `+`: 1개 이상
- `*`: 0개 이상
- `{3}`: 정확히 3개
- `[abc]`: a, b, c 중 하나
- `[0-9]`: 0부터 9까지

**문제 1-4: 로그 파일 분석**

`server_log.txt` 파일을 읽어서:
1. 모든 IP 주소를 추출 (형식: xxx.xxx.xxx.xxx)
2. 각 IP별로 접속 횟수를 카운트
3. ERROR 라인에서 에러 메시지만 추출
4. 결과를 딕셔너리로 만들어 `log_analysis.json`에 저장

**JSON 구조 예시:**
```json
{
  "ip_counts": {
    "192.168.1.1": 5,
    "10.0.0.15": 3
  },
  "errors": [
    "Connection timeout",
    "Database error"
  ]
}
```

---

### 1-5. 파일 경로 처리 (`pathlib` 모듈)

**배경 지식:**
`pathlib`는 파일과 디렉토리를 다루는 현대적인 방법입니다.
`os.path`보다 직관적이고 객체 지향적입니다.

**기본 사용법:**
```python
from pathlib import Path

# 경로 생성
file = Path('data/test.txt')
folder = Path('output')

# 존재 확인
if file.exists():
    print("파일이 있습니다")

# 파일 정보
print(file.name)       # 'test.txt'
print(file.stem)       # 'test' (확장자 제외)
print(file.suffix)     # '.txt'
print(file.parent)     # 'data'

# 경로 조합
new_path = folder / 'result.json'  # 'output/result.json'

# 디렉토리 생성
folder.mkdir(exist_ok=True)  # 이미 있어도 에러 안남

# 파일 목록
for f in Path('.').glob('*.txt'):  # 현재 폴더의 모든 .txt 파일
    print(f.name)

for f in Path('.').rglob('*.py'):  # 하위 폴더 포함 모든 .py 파일
    print(f)

# 파일 읽기/쓰기
text = file.read_text(encoding='utf-8')
file.write_text('내용', encoding='utf-8')
```

**문제 1-5: 파일 정리 도구**

현재 폴더에 여러 `.txt` 파일들이 있습니다:
1. `data` 폴더의 모든 `.txt` 파일을 찾기
2. 각 파일의 단어 수를 세기 (공백으로 구분)
3. 파일 크기(바이트)도 함께 기록
4. 결과를 딕셔너리로 만들어 `file_stats.json`에 저장

**JSON 구조 예시:**
```json
{
  "data/file1.txt": {
    "word_count": 150,
    "size_bytes": 1024
  },
  "data/file2.txt": {
    "word_count": 203,
    "size_bytes": 1536
  }
}
```

---

## 🌐 Section 2: 외부 라이브러리 (실무 필수)

### 2-1. HTTP 요청 (`requests` 라이브러리)

**배경 지식:**
`requests`는 웹 API를 호출하고 웹사이트에서 데이터를 가져오는 데 사용됩니다.
실무에서 외부 서비스와 연동할 때 필수적인 라이브러리입니다.

**기본 사용법:**
```python
import requests

# GET 요청 (데이터 가져오기)
response = requests.get('https://api.example.com/users')
print(response.status_code)  # 200이면 성공

if response.status_code == 200:
    data = response.json()  # JSON을 딕셔너리로 변환
    print(data)

# POST 요청 (데이터 전송)
payload = {'name': '홍길동', 'age': 25}
response = requests.post('https://api.example.com/users', json=payload)

# 헤더 추가
headers = {'Authorization': 'Bearer YOUR_TOKEN'}
response = requests.get('https://api.example.com/data', headers=headers)

# 쿼리 파라미터
params = {'page': 1, 'limit': 10}
response = requests.get('https://api.example.com/items', params=params)
```

**문제 2-1: 공공 API 활용**

JSONPlaceholder API(https://jsonplaceholder.typicode.com)를 사용하여:

1. `/users` 엔드포인트에서 모든 사용자 정보를 가져오기
2. 각 사용자의 게시글 수를 `/posts?userId={id}`로 조회
3. 사용자 정보 + 게시글 수를 합쳐서 새로운 데이터 생성
4. `user_posts_stats.json`에 저장

**결과 예시:**
```json
[
  {
    "id": 1,
    "name": "Leanne Graham",
    "email": "Sincere@april.biz",
    "post_count": 10
  },
  ...
]
```

**힌트:**
- 사용자마다 별도의 요청이 필요합니다 (반복문 사용)
- `len(posts)` 로 게시글 수를 셀 수 있습니다

---

### 2-2. 환율 정보 수집

**문제 2-2: 환율 데이터 수집**

Exchange Rate API(https://api.exchangerate-api.com/v4/latest/USD)를 사용하여:

1. 현재 USD 기준 환율 정보를 가져오기
2. KRW(한국), JPY(일본), EUR(유럽), CNY(중국) 환율만 추출
3. 각 통화로 1000 USD를 환전했을 때의 금액 계산
4. 결과를 보기 좋게 포맷하여 `exchange_rates.txt`에 저장

**출력 예시:**
```
환율 정보 (기준: USD 1,000)
====================
KRW: 1,320,500원
JPY: 147,230엔
EUR: 920유로
CNY: 7,245위안
====================
조회 시간: 2024-01-15 14:30:25
```

---

### 2-3. 데이터 분석 (`pandas` 라이브러리)

**배경 지식:**
`pandas`는 데이터 분석의 핵심 라이브러리입니다.
엑셀처럼 표 형태의 데이터를 다루며, 데이터 정제, 통계, 시각화의 기초가 됩니다.

**기본 사용법:**
```python
import pandas as pd

# CSV 읽기
df = pd.read_csv('data.csv')

# 기본 정보
print(df.head())      # 처음 5행
print(df.info())      # 데이터 타입, null 개수
print(df.describe())  # 통계 정보

# 컬럼 선택
ages = df['age']
subset = df[['name', 'age']]

# 필터링
adults = df[df['age'] >= 20]
filtered = df[(df['age'] >= 20) & (df['city'] == '서울')]

# 정렬
sorted_df = df.sort_values('age', ascending=False)

# 그룹화 및 집계
grouped = df.groupby('city')['age'].mean()
summary = df.groupby('department').agg({
    'salary': ['mean', 'sum'],
    'name': 'count'
})

# 새 컬럼 추가
df['age_group'] = df['age'].apply(lambda x: '청년' if x < 30 else '장년')

# 결측치 처리
df.dropna()  # 제거
df.fillna(0)  # 0으로 채우기

# 저장
df.to_csv('output.csv', index=False, encoding='utf-8-sig')
df.to_excel('output.xlsx', index=False)
```

**문제 2-3: 직원 데이터 분석**

`employees.csv` 파일을 pandas로 읽어서:

1. 부서(department)별 평균 연봉 계산
2. 재직기간(years_of_service) 5년 이상인 직원 필터링
3. 성과평가(performance)가 'Excellent'인 직원의 평균 연봉
4. 다음 정보를 포함하는 보고서를 `salary_report.csv`에 저장:
   - 부서명
   - 직원 수
   - 평균 연봉
   - 최고 연봉
   - 최저 연봉

**추가 도전:**
5. 각 부서에서 연봉 상위 1명의 이름을 추가로 표시

---

### 2-4. 복합 데이터 분석

**문제 2-4: 온라인 쇼핑몰 분석**

`orders.csv`와 `products.csv` 파일을 사용하여:

**orders.csv 구조:**
- order_id, customer_id, product_id, quantity, order_date

**products.csv 구조:**
- product_id, product_name, category, price

**분석 내용:**
1. 두 파일을 product_id로 결합(merge)
2. 주문별 총 금액(quantity × price) 계산
3. 카테고리별 매출액 통계
4. 2024년 1월 데이터만 필터링
5. 일별 매출액 추이를 계산
6. 결과를 `sales_analysis.xlsx`에 여러 시트로 저장:
   - Sheet1: 카테고리별 매출
   - Sheet2: 일별 매출
   - Sheet3: 베스트 상품 Top 10

**힌트:**
```python
# 파일 결합
merged = pd.merge(orders, products, on='product_id')

# 날짜 변환
df['order_date'] = pd.to_datetime(df['order_date'])

# 연월 추출
df['year'] = df['order_date'].dt.year
df['month'] = df['order_date'].dt.month

# 여러 시트로 저장
with pd.ExcelWriter('output.xlsx') as writer:
    df1.to_excel(writer, sheet_name='Sheet1')
    df2.to_excel(writer, sheet_name='Sheet2')
```

---

## 🎯 Section 3: 종합 프로젝트

### 프로젝트: 데이터 수집 및 분석 파이프라인

**시나리오:**
영화 정보 사이트에서 데이터를 수집하고 분석하는 자동화 스크립트를 만듭니다.

**요구사항:**

1. **데이터 수집 (requests)**
   - OMDb API 사용 (http://www.omdbapi.com/)
   - API Key 발급 필요 (무료)
   - `movies.txt`에 있는 영화 제목들로 검색
   - 각 영화의 제목, 연도, 평점, 장르, 감독 정보 수집

2. **데이터 정제 (pandas)**
   - 검색 실패한 영화 제외
   - 평점을 숫자로 변환 (N/A 처리)
   - 장르를 리스트로 분리

3. **데이터 분석**
   - 연도별 영화 수
   - 장르별 평균 평점
   - 평점 상위 10개 영화
   - 감독별 영화 수

4. **결과 저장**
   - 원본 데이터: `movies_data.csv`
   - 분석 결과: `movies_analysis.json`
   - 보고서: `movies_report.txt` (보기 좋게 포맷팅)

5. **로깅**
   - 실행 시간 기록
   - 성공/실패한 영화 수 출력
   - 에러 발생 시 로그에 기록

**보너스:**
- 진행 상황을 표시하는 프로그레스 바 추가
- 캐싱 기능 (이미 조회한 영화는 다시 요청하지 않기)
- 커맨드라인 인자로 API키와 입력 파일 받기

---

## 📝 학습 팁

1. **단계별로 진행하세요**
   - 한 번에 완성하려 하지 말고, 작은 부분부터 테스트

2. **에러 메시지를 읽으세요**
   - Python 에러 메시지는 매우 친절합니다
   - 어느 줄에서 에러가 났는지 정확히 알려줍니다

3. **print()를 적극 활용하세요**
   - 중간 결과를 확인하면서 진행
   - 디버깅의 기본

4. **공식 문서를 참고하세요**
   - Python 내장 모듈: https://docs.python.org/ko/3/library/
   - pandas: https://pandas.pydata.org/docs/
   - requests: https://requests.readthedocs.io/

5. **막힐 때는**
   - 에러 메시지를 구글에 검색
   - Stack Overflow 확인
   - 작은 단위로 쪼개서 테스트

---

## ✅ 체크리스트

각 문제를 풀 때마다 확인하세요:

- [ ] 파일을 올바르게 읽었나?
- [ ] 데이터 타입이 적절한가?
- [ ] 예외 처리를 했나? (파일이 없을 때 등)
- [ ] 결과가 요구사항과 일치하나?
- [ ] 코드에 주석을 달았나?
- [ ] 함수로 분리할 수 있는 부분은 없나?

---

**화이팅! 실전 연습이 실력 향상의 지름길입니다! 💪**

답안이 필요할 때는 `python_practice_answers.md` 파일을 확인하세요.
